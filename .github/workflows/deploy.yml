name: Deploy

on:
  release:
    types:
      - published

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment: production

    steps:
      - name: Deploy to Coolify
        env:
          DEPLOY_URL: ${{ vars.DEPLOY_URL }}
        run: curl $DEPLOY_URL